on:
  push: {}

jobs:
  conda-ci:
    runs-on: ubuntu-22.04
    steps:
      - name: Git checkout
        uses: actions/checkout@v2

      - name: Docker
        run: |
          docker build -t conda -f docker/Dockerfile.conda docker/
          docker run --rm -it -v $PWD:/code --workdir=/code conda cargo test
